<span>
  <%= t("sortBy") %> 
  <%= render :partial => "share/search/sortlink", :locals => {
      :search => $search,
      :field => 'title',
      :title => t("order.title"),
      :params => params
    }
  %>
  <%= render :partial => "share/search/sortlink", :locals => {
      :search => $search,
      :field => 'created_at',
      :title => t("order.date"),
      :params => params
    }
  %>
  <%= render :partial => "share/search/sortlink", :locals => {
      :search => $search,
      :field => 'locations_address',
      :title => t("order.location"),
      :params => params
    }
  %>
</span>
