<span>
  <%= t("sortBy") %> 
  <%= render :partial => "share/search/sortlink", :locals => {
      :search => $search,
      :field => 'title',
      :title => t("order.title"),
      :params => params[:search]
    }
  %>
  <%= render :partial => "share/search/sortlink", :locals => {
      :search => $search,
      :field => 'created_at',
      :title => t("order.date"),
      :params => params[:search]
    }
  %>
  <%= render :partial => "share/search/sortlink", :locals => {
      :search => $search,
      :field => 'location_city',
      :title => t("order.location"),
      :params => params[:search]
    }
  %>
</span>