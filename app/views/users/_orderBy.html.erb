<span>
  <%= t("sortBy") %> 
  <%= render :partial => "share/search/sortlink", :locals => {
      :search => @usersearch,
      :field => 'login',
      :title => t("order.user")
    }
  %>
  ,
  <%= render :partial => "share/search/sortlink", :locals => {
      :search => @usersearch,
      :field => 'locations_address',
      :title => t("order.location")
    }
  %>
</span>
