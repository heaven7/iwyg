<tr class="ping font-smaller">
    <td><%= group.images.first.image.url if group.images.size > 0 %> <%= link_to group.title, group %></td>
    <td><%= render :partial => "groupmenu", :locals => { :group => group} if logged_in? %></td>
</tr>
