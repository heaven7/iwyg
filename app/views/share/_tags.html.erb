<% if !tags.empty? %>
  <p class="font-smaller">
  <%= t("tags") %>:
  <% for tag in tags %>
      <span><%= link_to tag, :controller => model, "q[tag]" => tag.name %></span>
    <% end %>
  </p>
<% end %>
