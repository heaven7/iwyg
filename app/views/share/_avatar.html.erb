<% if model %>
<div id="avatar"> 
  <span>
  	<% if model.images.first %>
    	<%= link_to image_tag(smallThumb(model)), model.images.first.image.url, class: "colorbox" %>
    <% else %>
    	<%= image_tag smallThumb(model) %>
    <% end %>
  </span>
</div>
<% end %>
