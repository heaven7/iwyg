<% if user %>
  <div class="mircro-user-thumb bordered font-smaller center ui-corner-all">
    <%= link_to_unless_current image_tag(microThumb(user), :alt => user.login, :title => user.login), profile_path(user.login) %>
    <br />
    <%= link_to user.login, profile_path(user.login) %>
  </div>
<% end %>