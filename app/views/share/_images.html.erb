<% if images %>
  <div class="list images">  
    <% images.each do |i| %>
      <% if !i.image.url.include? "missing" %>
        <span class="image">
          <%= image_tag i.image.url(:small) %>
        </span>
     <% end %>
    <% end %>
  </div>
<% end %>
